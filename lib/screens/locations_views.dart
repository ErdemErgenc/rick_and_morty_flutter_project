import 'package:flutter/material.dart';

class LocationsViews extends StatelessWidget {
  const LocationsViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mekanlar"));
  }
}
